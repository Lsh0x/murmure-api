[workspace]
members = [
    "murmure-tts",
    "murmure-server",
    "examples",
]
resolver = "2"

[workspace.dependencies]
# Common dependencies can be shared here if needed
# For now, each crate manages its own dependencies

